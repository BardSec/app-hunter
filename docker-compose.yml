version: "3.9"

services:
  web:
    build: .
    ports:
      - "33333:33333"
    volumes:
      - .:/app
      - app_data:/app/data
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
      - PYTHONDONTWRITEBYTECODE=1
    restart: unless-stopped

volumes:
  app_data:
